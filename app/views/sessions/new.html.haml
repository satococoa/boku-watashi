%h1=t(:child)+t(:login)

= form_tag sessions_path do
  %p
    =text_field_tag :childname, params[:childname], :placeholder => t(:childname)
    
  %p
    =password_field_tag :password, '', :placeholder => t(:password)
    
  %p.button
    =submit_tag t(:login)
