%h1 Listing wishes

%p.progress
  %progress{:max => current_user.pocket, :value => current_user.wishes.approved.monthly(params[:ym]).sum(:cost)}
    = number_with_delimiter(current_user.wishes.approved.monthly(params[:ym]).sum(:cost))+' 円'
    \/
    = number_with_delimiter(current_user.pocket)+' 円'
  %span
    = number_with_delimiter(current_user.wishes.approved.monthly(params[:ym]).sum(:cost))+' 円'
    \/
    = number_with_delimiter(current_user.pocket)+' 円'

%table
  %tr
    %th Child
    %th Cost
    %th Purpose
    %th Status
    %th
    %th
    %th

  - @wishes.each do |wish|
    %tr
      %td= wish.child_id
      %td= number_with_delimiter(wish.cost)+' 円'
      %td= wish.purpose
      %td= Wish::Status[wish.status]
      %td= link_to 'Show', wish
      %td= link_to 'Edit', edit_wish_path(wish)
      %td= link_to 'Destroy', wish, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Wish', new_wish_path
